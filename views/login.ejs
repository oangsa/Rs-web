<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Itim&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/bs4/dt-1.13.1/fc-4.2.1/r-2.4.0/datatables.min.css"/>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <title>Login</title>
</head>
<style>
    .swal2-popup {
        font-family: 'Itim';
    }
</style>
<body class="container" style="background-color: lightblue; padding: 3rem 0rem 3rem; margin-top: 50px">
    <div class="card" style="font-family: 'Itim'; background-color: white ;border-radius: 0.5rem; width: 100%; padding: 20px;">
        <div class="card-header">
            Login (Admin Access)
        </div>
        <div class="card-body">
            <form action="/admin/gologin" method="post">
                <div class="form-group">
                    <label>Username:</label>
                    <input type="text" class="form-control" name="name" value="<%= name %>" autocomplete="off" id="name">
                </div>
                <div class="form-group">
                    <label>Password:</label>
                    <input type="password" class="form-control" name="pass" value="<%= pass %>" autocomplete="off" id="pass">
                </div>
                <div class="form-group">
                    <button class="btn btn-info">Login</button>
                </div>
            </form>
            <div>
                <form action="/" method="GET">
                    <div class="form-group">
                        <button class="btn btn-danger">ย้อนกลับ</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <% if (locals.sendAlert) { %> 
        <script>
          Swal.fire({
            icon: "<%= icon %>",
            title: "<%= title %>",
            text: "<%= msg %>",
            showConfirmButton: true,
          })
       </script>
       <% } %>
</body>
</html>
