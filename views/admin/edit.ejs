<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Itim&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <title>Add Student</title>
</head>
<style>
    .swal2-popup {
        font-family: 'Itim';
    }
</style>
<body class="container" style="background-color: lightblue; padding: 3rem 0rem 3rem; margin-top: 50px">
    <div class="card" style="font-family: 'Itim'; background-color: white ;border-radius: 0.5rem; width: 100%; padding: 20px;">
        <div class="card-header">
            Edit Student
        </div>
        <div class="card-body">
            <form action="/admin/edit/<%= number %>" method="post">
                <div class="form-group">
                    <label>ชื่อ-นามสกุล:</label>
                    <input type="text" class="form-control" name="name" value="<%= name %>" autocomplete="off">
                </div><p></p>
                <div class="form-group">
                    <label>เลขที่:</label>
                    <input type="text" class="form-control" name="number" value="<%= number %>" autocomplete="off">
                </div><p></p>
                <div class="form-group">
                    <button class="btn btn-info">Update</button>
                </div>
            </form><p></p>
            <div>
                <form action="/admin" method="GET">
                    <div class="form-group">
                        <button class="btn btn-danger">ย้อนกลับ</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <% if (locals.sendAlert) { %> 
        <script>
          Swal.fire({
            icon: "<%= icon %>",
            title: "<%= title %>",
            text: "<%= msg %>",
            showConfirmButton: true,
          })
       </script>
       <% } %>
</body>
</html>
